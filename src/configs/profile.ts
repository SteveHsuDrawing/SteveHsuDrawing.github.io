/**
 * Author profile constants.
 *
 * Personal info of the site author (Steve Hsu / 什五).  Extend this
 * module per need — keep it minimal, one constant per piece of data.
 */

/** Author's birth month (September) — drives the birthday-week cover. */
export const BIRTHDAY_MONTH = 9;

/** Author's birth day (the 8th) — drives the birthday-week cover. */
export const BIRTHDAY_DAY = 8;
